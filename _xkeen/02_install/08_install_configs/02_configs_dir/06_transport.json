// Настройки транспортного протокола
// *Используются, если не определены настройки транспорта конкретного соединения в файле "outbounds" в секции "streamSettings".
// **Настройки транспорта клиентской и серверной конфигурации должны совпадать
{
  "transport": {
    "tcpSettings": {},
    "kcpSettings": {},
    "wsSettings": {},
    "httpSettings": {},
    "quicSettings": {},
    "dsSettings": {},
    "grpcSettings": {}
  }
}