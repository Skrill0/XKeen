// Настройка входящих соединений
//
// Доступные протоколы: 
// 1. Dokodemo-door — Транзитный протокол. 
// Используется для перенаправления трафика внутри конфигурации inbounds.
//
// address — IP или доменное имя, куда пересылаются данные. Не требуется, если используется параметр "followRedirect" для iptables.
// Пример записи: "address": "8.8.8.8" или "address": "keenetic.com"
//
// port — порт, на который будут пересылаться данные. 
// Пример записи:  "port": 53
//
// network — какие протоколы будут перенаправляться
// Пример записи:  "network": "tcp,udp"
// tcp — перенаправляет только tcp пакеты. udp игнорирует.
// udp — перенаправляет только udp пакеты. tcp игнорирует.
// tcp,udp — перенаправляет tcp и udp пакеты.
// 
// timeout — время в секундах, через которое соединение будет разорвано.
// Пример записи: "timeout": 300
// Используется для оптимизации. Чтобы освобождать ресурсы для новых соединений.
// Значение поумолчанию 300. Если Вас устраивает — не стоит добавлять в конфигурацию аргумент "timeout".
//
// followRedirect — использовать ли настройки iptables
// Пример записи: "followRedirect": false
// true — использует
// false — не использует
// Значение поумолчанию false. Если Вас устраивает — не стоит добавлять в конфигурацию аргумент "followRedirect".
// **Рекомендуется к ознакомлению использование с transparent proxy он же tproxy.
//
// userLevel — использование профиля из policy.
// Пример записи: "userLevel": 0
// Используется для настройки профилей пользователей. 
// К примеру, пользователей с уровнем 0 мы можем перенаправлять на 8.8.8.8, а с уровнем 1 на 77.88.8.8.
//
//
// 2. HTTP